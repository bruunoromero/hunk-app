import React from "react";
import { Header, Body, Title, Left, Right } from "native-base";

export default () => (
  <Header>
    <Left />
    <Body>
      <Title>Header</Title>
    </Body>
    <Right />
  </Header>
);
